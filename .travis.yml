dist: trusty
sudo: false

language: node_js
node_js:
  - '8'

addons:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

cache:
  directories:
    - ./node_modules

install:
  - npm install

nv:
  - TEST_DIR=/home/travis/build/veitej/Project2-Gradebook/front-end/Project2
 

script:
  - cd $TEST_DIR && npm make test -- --watch=false --no-progress --browsers=ChromeHeadlessNoSandbox
  
  notifications:
  email: roberte.flaherty@gmail.com
  email: erick.hernandez@revature.net
  email: christian.torre.1@gmail.com
